let lado = 5, largura = 16, comprimento = 6, base = 12, altura = 8, resposta = 0

let areaDoQuadrado = Math.pow(lado, 2)
console.log(`area do quadrado ${areaDoQuadrado}`)

let areaDoRetangulo = largura *= comprimento
console.log(`area do retangulo ${areaDoRetangulo}`)

let areaDoTriangulo = base *= altura / 2
console.log(`area do Triangulo ${areaDoTriangulo}`)